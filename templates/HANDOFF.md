# New-task handoff pointer

Last reviewed: `<last-reviewed>`

This file is an optional compatibility pointer. It does not own project state,
authorization, repository identity, or a second reading sequence.

Start with `AGENTS.md`; it owns global behavior and routes any applicable
specialized policy.

Then follow `PROJECT_MEMORY.md` for durable-state reading. Read the active
`PROJECT_OPERATIONS.md` overlay and any task checkpoint only when those
authoritative owners require them.

Do not copy project status, repository identities, authorization rules,
commands, or another reading sequence into this file.
