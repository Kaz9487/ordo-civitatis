# Current project state

Project facts last reviewed: `<last-reviewed>`

Live source, official contracts, and original evidence remain stronger
authorities than this handoff summary.

## At a glance

| Area | Current state |
|---|---|
| Product or service | `<current supported state>` |
| Research or development | `<current decision>` |
| Release or deployment | `<current milestone>` |
| External claim limits | `<not established or unavailable>` |
| Active resumable task | `<none recorded or task identifier>` |
| Required next action | `<none or conditional next action>` |

## Current repository and environment identity

The operations overlay owns canonical roles and procedure. This table records
the current identities used by reviewed project state; it does not redefine
the operational policy.

| Role | Repository or system | Current identity |
|---|---|---|
| Source of truth | `<source-of-truth-repository>` | `<commit-or-tree>` |
| Mirror, if applicable | `<mirror-repository>` | `<commit-or-tree>` |
| Evaluation environment | `<environment>` | `<environment-identity>` |

## Current routes or supported operations

- `<supported operation and its boundary>`

## Confirmed evidence

### Evidence and artifact locators

This table is navigation only. Original result artifacts remain the evidence
body; identity and validation records support their stated narrower purposes.

| Role | Artifact | Identity | Claim scope |
|---|---|---|---|
| `<result>` | `<artifact-path>` | `<artifact-sha256>` | `<supported claim>` |

### Confirmed conclusions

- `<conclusion and evidence status>`

## Claim limits

Current evidence does not establish:

- `<unsupported or unavailable claim>`

## Current decisions

1. `<decision and condition for reconsideration>`

## Active task and conditional next action

`<none recorded, or link to active task checkpoint>`

Start new material work only when `<trigger, changed condition, or new
hypothesis>` exists.
